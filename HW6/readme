# HW6 Assignment
This repository contains the HW6 assignment using localpackages with a submodule 'calculus'`, imported via `PYTHONPATH`
## Directory Structure
- `localpackages/`: Top-level custom package.
  - `__init__.py`: Marks the directory as a package.
  - `calculus/`: Submodule for calc opoerations
    - `__init__.py`: Marks the directory as a submodule.
    - `ODE.py`: Contains the function fourth_runge_kutta, which uses the fourth order Runge Kutta method to solve a first order ODE and graph the result
- `Homework6.py`: Imports the function from ODE.py and uses the package to solve three problems.
## Setup Instructions
1. Clone this repository to your local machine.
2. Open the `HW6` folder in VS Code.
3. Set the `PYTHONPATH` to include the `HW6` directory:
   - In the terminal: `export PYTHONPATH=$PYTHONPATH:/path/to/HW6`
4. Run `main_script.py`:
   - In the terminal: `python Homework6.py`